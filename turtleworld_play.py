from swampy.TurtleWorld import *


world = TurtleWorld()
alice = Turtle()

def snow_flake_side(length, level):
	""" Draw a side of the snowflake curve with 
	side length length and recursion depth of level"""

	# fd(alice, length/3.0)
	# lt(alice,-60)
	# fd(alice, length/3.0)
	# lt(alice, 120)
	# fd(alice, length/3.0)
	# lt(alice,-60)
	# fd(alice, length/3.0)
	# lt(alice,120)

	#base case 
	#if level > 1 
	#new depth level - 1 

	if i = 4 

	wait_for_user()

snow_flake_side(50, 1)


def snow_flake():
	for i in range(6)
		snow_flake_side(50,1)
